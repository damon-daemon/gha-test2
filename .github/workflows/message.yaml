name: Print Message
on:
  workflow_dispatch: {}
    # inputs:
    #   serviceId:
    #     required: true
    #     description: 'Deploy parameters'
    #   semanticVersion:
    #     required: true
    #   repositoryName:
    #     required: true
    #   awsRegion:
    #     required: true
    #   cdkOutZipSha:
    #     required: true

# env:
#   DEPLOY_PARAMETERS: ${{ inputs.deployParameters }}

# jobs:
#   create-output:
#     runs-on: ubuntu-latest
#     outputs: 
#       CdkOutZipSha: ${{ steps.create-output.outputs.CDK_OUT_ZIP_SHA256 }}
#     steps:
#       - name: Create output
#         id: create-output
#         run: |
#           echo "CDK_OUT_ZIP_SHA256=value" >> $GITHUB_OUTPUT
#   echo-output:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Echo output
#         run: echo ${{ needs.create-output.outputs.CdkOutZipSha }}

jobs:
  add-cdk-out-to-release:
    runs-on: ubuntu-latest
    # Map a step output to a job output
    outputs:
      CdkOutZipSha: ${{ steps.get-cdk-out-zip-sha.outputs.CDK_OUT_ZIP_SHA256 }}
    steps:
      - name: Get sha256sum of cdk.out.zip
        id: get-cdk-out-zip-sha
        run: echo "CDK_OUT_ZIP_SHA256=$(sha256sum discover/cdk.out.zip)" >> $GITHUB_OUTPUT

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - run: echo ${{needs.add-cdk-out-to-release.outputs.CdkOutZipSha}}
